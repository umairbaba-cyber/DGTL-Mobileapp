import React from 'react';
import {  View} from 'react-native';

//it is component which is used to scan qrcode 
export default function BarCodeScan({ navigation,showBorderBottom,qrCodePressed }) {
//navigation is passed as reference to move from one screen to other screen
// showBorderBottom is passed to show border as in some screen design there is no border to show
//qrCodePressed a function which will be called on when qrcode button is pressed
    return (
     
        <View/>
     
    );

}

